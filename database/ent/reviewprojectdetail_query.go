// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/DemoonLXW/up_learning/database/ent/predicate"
	"github.com/DemoonLXW/up_learning/database/ent/reviewproject"
	"github.com/DemoonLXW/up_learning/database/ent/reviewprojectdetail"
)

// ReviewProjectDetailQuery is the builder for querying ReviewProjectDetail entities.
type ReviewProjectDetailQuery struct {
	config
	ctx               *QueryContext
	order             []reviewprojectdetail.OrderOption
	inters            []Interceptor
	predicates        []predicate.ReviewProjectDetail
	withReviewProject *ReviewProjectQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ReviewProjectDetailQuery builder.
func (rpdq *ReviewProjectDetailQuery) Where(ps ...predicate.ReviewProjectDetail) *ReviewProjectDetailQuery {
	rpdq.predicates = append(rpdq.predicates, ps...)
	return rpdq
}

// Limit the number of records to be returned by this query.
func (rpdq *ReviewProjectDetailQuery) Limit(limit int) *ReviewProjectDetailQuery {
	rpdq.ctx.Limit = &limit
	return rpdq
}

// Offset to start from.
func (rpdq *ReviewProjectDetailQuery) Offset(offset int) *ReviewProjectDetailQuery {
	rpdq.ctx.Offset = &offset
	return rpdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rpdq *ReviewProjectDetailQuery) Unique(unique bool) *ReviewProjectDetailQuery {
	rpdq.ctx.Unique = &unique
	return rpdq
}

// Order specifies how the records should be ordered.
func (rpdq *ReviewProjectDetailQuery) Order(o ...reviewprojectdetail.OrderOption) *ReviewProjectDetailQuery {
	rpdq.order = append(rpdq.order, o...)
	return rpdq
}

// QueryReviewProject chains the current query on the "review_project" edge.
func (rpdq *ReviewProjectDetailQuery) QueryReviewProject() *ReviewProjectQuery {
	query := (&ReviewProjectClient{config: rpdq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rpdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rpdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(reviewprojectdetail.Table, reviewprojectdetail.FieldID, selector),
			sqlgraph.To(reviewproject.Table, reviewproject.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, reviewprojectdetail.ReviewProjectTable, reviewprojectdetail.ReviewProjectColumn),
		)
		fromU = sqlgraph.SetNeighbors(rpdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ReviewProjectDetail entity from the query.
// Returns a *NotFoundError when no ReviewProjectDetail was found.
func (rpdq *ReviewProjectDetailQuery) First(ctx context.Context) (*ReviewProjectDetail, error) {
	nodes, err := rpdq.Limit(1).All(setContextOp(ctx, rpdq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{reviewprojectdetail.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rpdq *ReviewProjectDetailQuery) FirstX(ctx context.Context) *ReviewProjectDetail {
	node, err := rpdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ReviewProjectDetail ID from the query.
// Returns a *NotFoundError when no ReviewProjectDetail ID was found.
func (rpdq *ReviewProjectDetailQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = rpdq.Limit(1).IDs(setContextOp(ctx, rpdq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{reviewprojectdetail.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rpdq *ReviewProjectDetailQuery) FirstIDX(ctx context.Context) int {
	id, err := rpdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ReviewProjectDetail entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ReviewProjectDetail entity is found.
// Returns a *NotFoundError when no ReviewProjectDetail entities are found.
func (rpdq *ReviewProjectDetailQuery) Only(ctx context.Context) (*ReviewProjectDetail, error) {
	nodes, err := rpdq.Limit(2).All(setContextOp(ctx, rpdq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{reviewprojectdetail.Label}
	default:
		return nil, &NotSingularError{reviewprojectdetail.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rpdq *ReviewProjectDetailQuery) OnlyX(ctx context.Context) *ReviewProjectDetail {
	node, err := rpdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ReviewProjectDetail ID in the query.
// Returns a *NotSingularError when more than one ReviewProjectDetail ID is found.
// Returns a *NotFoundError when no entities are found.
func (rpdq *ReviewProjectDetailQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = rpdq.Limit(2).IDs(setContextOp(ctx, rpdq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{reviewprojectdetail.Label}
	default:
		err = &NotSingularError{reviewprojectdetail.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rpdq *ReviewProjectDetailQuery) OnlyIDX(ctx context.Context) int {
	id, err := rpdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ReviewProjectDetails.
func (rpdq *ReviewProjectDetailQuery) All(ctx context.Context) ([]*ReviewProjectDetail, error) {
	ctx = setContextOp(ctx, rpdq.ctx, "All")
	if err := rpdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ReviewProjectDetail, *ReviewProjectDetailQuery]()
	return withInterceptors[[]*ReviewProjectDetail](ctx, rpdq, qr, rpdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rpdq *ReviewProjectDetailQuery) AllX(ctx context.Context) []*ReviewProjectDetail {
	nodes, err := rpdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ReviewProjectDetail IDs.
func (rpdq *ReviewProjectDetailQuery) IDs(ctx context.Context) (ids []int, err error) {
	if rpdq.ctx.Unique == nil && rpdq.path != nil {
		rpdq.Unique(true)
	}
	ctx = setContextOp(ctx, rpdq.ctx, "IDs")
	if err = rpdq.Select(reviewprojectdetail.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rpdq *ReviewProjectDetailQuery) IDsX(ctx context.Context) []int {
	ids, err := rpdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rpdq *ReviewProjectDetailQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rpdq.ctx, "Count")
	if err := rpdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rpdq, querierCount[*ReviewProjectDetailQuery](), rpdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rpdq *ReviewProjectDetailQuery) CountX(ctx context.Context) int {
	count, err := rpdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rpdq *ReviewProjectDetailQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rpdq.ctx, "Exist")
	switch _, err := rpdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rpdq *ReviewProjectDetailQuery) ExistX(ctx context.Context) bool {
	exist, err := rpdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ReviewProjectDetailQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rpdq *ReviewProjectDetailQuery) Clone() *ReviewProjectDetailQuery {
	if rpdq == nil {
		return nil
	}
	return &ReviewProjectDetailQuery{
		config:            rpdq.config,
		ctx:               rpdq.ctx.Clone(),
		order:             append([]reviewprojectdetail.OrderOption{}, rpdq.order...),
		inters:            append([]Interceptor{}, rpdq.inters...),
		predicates:        append([]predicate.ReviewProjectDetail{}, rpdq.predicates...),
		withReviewProject: rpdq.withReviewProject.Clone(),
		// clone intermediate query.
		sql:  rpdq.sql.Clone(),
		path: rpdq.path,
	}
}

// WithReviewProject tells the query-builder to eager-load the nodes that are connected to
// the "review_project" edge. The optional arguments are used to configure the query builder of the edge.
func (rpdq *ReviewProjectDetailQuery) WithReviewProject(opts ...func(*ReviewProjectQuery)) *ReviewProjectDetailQuery {
	query := (&ReviewProjectClient{config: rpdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rpdq.withReviewProject = query
	return rpdq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ReviewProjectID uint32 `json:"review_project_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ReviewProjectDetail.Query().
//		GroupBy(reviewprojectdetail.FieldReviewProjectID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rpdq *ReviewProjectDetailQuery) GroupBy(field string, fields ...string) *ReviewProjectDetailGroupBy {
	rpdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ReviewProjectDetailGroupBy{build: rpdq}
	grbuild.flds = &rpdq.ctx.Fields
	grbuild.label = reviewprojectdetail.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ReviewProjectID uint32 `json:"review_project_id,omitempty"`
//	}
//
//	client.ReviewProjectDetail.Query().
//		Select(reviewprojectdetail.FieldReviewProjectID).
//		Scan(ctx, &v)
func (rpdq *ReviewProjectDetailQuery) Select(fields ...string) *ReviewProjectDetailSelect {
	rpdq.ctx.Fields = append(rpdq.ctx.Fields, fields...)
	sbuild := &ReviewProjectDetailSelect{ReviewProjectDetailQuery: rpdq}
	sbuild.label = reviewprojectdetail.Label
	sbuild.flds, sbuild.scan = &rpdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ReviewProjectDetailSelect configured with the given aggregations.
func (rpdq *ReviewProjectDetailQuery) Aggregate(fns ...AggregateFunc) *ReviewProjectDetailSelect {
	return rpdq.Select().Aggregate(fns...)
}

func (rpdq *ReviewProjectDetailQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rpdq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rpdq); err != nil {
				return err
			}
		}
	}
	for _, f := range rpdq.ctx.Fields {
		if !reviewprojectdetail.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rpdq.path != nil {
		prev, err := rpdq.path(ctx)
		if err != nil {
			return err
		}
		rpdq.sql = prev
	}
	return nil
}

func (rpdq *ReviewProjectDetailQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ReviewProjectDetail, error) {
	var (
		nodes       = []*ReviewProjectDetail{}
		_spec       = rpdq.querySpec()
		loadedTypes = [1]bool{
			rpdq.withReviewProject != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ReviewProjectDetail).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ReviewProjectDetail{config: rpdq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rpdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := rpdq.withReviewProject; query != nil {
		if err := rpdq.loadReviewProject(ctx, query, nodes, nil,
			func(n *ReviewProjectDetail, e *ReviewProject) { n.Edges.ReviewProject = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (rpdq *ReviewProjectDetailQuery) loadReviewProject(ctx context.Context, query *ReviewProjectQuery, nodes []*ReviewProjectDetail, init func(*ReviewProjectDetail), assign func(*ReviewProjectDetail, *ReviewProject)) error {
	ids := make([]uint32, 0, len(nodes))
	nodeids := make(map[uint32][]*ReviewProjectDetail)
	for i := range nodes {
		fk := nodes[i].ReviewProjectID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(reviewproject.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "review_project_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (rpdq *ReviewProjectDetailQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rpdq.querySpec()
	_spec.Node.Columns = rpdq.ctx.Fields
	if len(rpdq.ctx.Fields) > 0 {
		_spec.Unique = rpdq.ctx.Unique != nil && *rpdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rpdq.driver, _spec)
}

func (rpdq *ReviewProjectDetailQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(reviewprojectdetail.Table, reviewprojectdetail.Columns, sqlgraph.NewFieldSpec(reviewprojectdetail.FieldID, field.TypeInt))
	_spec.From = rpdq.sql
	if unique := rpdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rpdq.path != nil {
		_spec.Unique = true
	}
	if fields := rpdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, reviewprojectdetail.FieldID)
		for i := range fields {
			if fields[i] != reviewprojectdetail.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if rpdq.withReviewProject != nil {
			_spec.Node.AddColumnOnce(reviewprojectdetail.FieldReviewProjectID)
		}
	}
	if ps := rpdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rpdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rpdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rpdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rpdq *ReviewProjectDetailQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rpdq.driver.Dialect())
	t1 := builder.Table(reviewprojectdetail.Table)
	columns := rpdq.ctx.Fields
	if len(columns) == 0 {
		columns = reviewprojectdetail.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rpdq.sql != nil {
		selector = rpdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rpdq.ctx.Unique != nil && *rpdq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rpdq.predicates {
		p(selector)
	}
	for _, p := range rpdq.order {
		p(selector)
	}
	if offset := rpdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rpdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ReviewProjectDetailGroupBy is the group-by builder for ReviewProjectDetail entities.
type ReviewProjectDetailGroupBy struct {
	selector
	build *ReviewProjectDetailQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rpdgb *ReviewProjectDetailGroupBy) Aggregate(fns ...AggregateFunc) *ReviewProjectDetailGroupBy {
	rpdgb.fns = append(rpdgb.fns, fns...)
	return rpdgb
}

// Scan applies the selector query and scans the result into the given value.
func (rpdgb *ReviewProjectDetailGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rpdgb.build.ctx, "GroupBy")
	if err := rpdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ReviewProjectDetailQuery, *ReviewProjectDetailGroupBy](ctx, rpdgb.build, rpdgb, rpdgb.build.inters, v)
}

func (rpdgb *ReviewProjectDetailGroupBy) sqlScan(ctx context.Context, root *ReviewProjectDetailQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rpdgb.fns))
	for _, fn := range rpdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rpdgb.flds)+len(rpdgb.fns))
		for _, f := range *rpdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rpdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rpdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ReviewProjectDetailSelect is the builder for selecting fields of ReviewProjectDetail entities.
type ReviewProjectDetailSelect struct {
	*ReviewProjectDetailQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rpds *ReviewProjectDetailSelect) Aggregate(fns ...AggregateFunc) *ReviewProjectDetailSelect {
	rpds.fns = append(rpds.fns, fns...)
	return rpds
}

// Scan applies the selector query and scans the result into the given value.
func (rpds *ReviewProjectDetailSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rpds.ctx, "Select")
	if err := rpds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ReviewProjectDetailQuery, *ReviewProjectDetailSelect](ctx, rpds.ReviewProjectDetailQuery, rpds, rpds.inters, v)
}

func (rpds *ReviewProjectDetailSelect) sqlScan(ctx context.Context, root *ReviewProjectDetailQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rpds.fns))
	for _, fn := range rpds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rpds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rpds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
