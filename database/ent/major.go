// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/DemoonLXW/up_learning/database/ent/college"
	"github.com/DemoonLXW/up_learning/database/ent/major"
)

// Major is the model entity for the Major schema.
type Major struct {
	config `json:"-"`
	// ID of the ent.
	ID uint16 `json:"id,omitempty"`
	// Cid holds the value of the "cid" field.
	Cid uint8 `json:"cid,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// IsDisabled holds the value of the "is_disabled" field.
	IsDisabled bool `json:"is_disabled,omitempty"`
	// CreatedTime holds the value of the "created_time" field.
	CreatedTime *time.Time `json:"created_time,omitempty"`
	// DeletedTime holds the value of the "deleted_time" field.
	DeletedTime *time.Time `json:"deleted_time,omitempty"`
	// ModifiedTime holds the value of the "modified_time" field.
	ModifiedTime *time.Time `json:"modified_time,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MajorQuery when eager-loading is set.
	Edges        MajorEdges `json:"edges"`
	selectValues sql.SelectValues
}

// MajorEdges holds the relations/edges for other nodes in the graph.
type MajorEdges struct {
	// College holds the value of the college edge.
	College *College `json:"college,omitempty"`
	// Classes holds the value of the classes edge.
	Classes []*Class `json:"classes,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// CollegeOrErr returns the College value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MajorEdges) CollegeOrErr() (*College, error) {
	if e.loadedTypes[0] {
		if e.College == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: college.Label}
		}
		return e.College, nil
	}
	return nil, &NotLoadedError{edge: "college"}
}

// ClassesOrErr returns the Classes value or an error if the edge
// was not loaded in eager-loading.
func (e MajorEdges) ClassesOrErr() ([]*Class, error) {
	if e.loadedTypes[1] {
		return e.Classes, nil
	}
	return nil, &NotLoadedError{edge: "classes"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Major) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case major.FieldIsDisabled:
			values[i] = new(sql.NullBool)
		case major.FieldID, major.FieldCid:
			values[i] = new(sql.NullInt64)
		case major.FieldName:
			values[i] = new(sql.NullString)
		case major.FieldCreatedTime, major.FieldDeletedTime, major.FieldModifiedTime:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Major fields.
func (m *Major) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case major.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			m.ID = uint16(value.Int64)
		case major.FieldCid:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field cid", values[i])
			} else if value.Valid {
				m.Cid = uint8(value.Int64)
			}
		case major.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				m.Name = value.String
			}
		case major.FieldIsDisabled:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_disabled", values[i])
			} else if value.Valid {
				m.IsDisabled = value.Bool
			}
		case major.FieldCreatedTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_time", values[i])
			} else if value.Valid {
				m.CreatedTime = new(time.Time)
				*m.CreatedTime = value.Time
			}
		case major.FieldDeletedTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_time", values[i])
			} else if value.Valid {
				m.DeletedTime = new(time.Time)
				*m.DeletedTime = value.Time
			}
		case major.FieldModifiedTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field modified_time", values[i])
			} else if value.Valid {
				m.ModifiedTime = new(time.Time)
				*m.ModifiedTime = value.Time
			}
		default:
			m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Major.
// This includes values selected through modifiers, order, etc.
func (m *Major) Value(name string) (ent.Value, error) {
	return m.selectValues.Get(name)
}

// QueryCollege queries the "college" edge of the Major entity.
func (m *Major) QueryCollege() *CollegeQuery {
	return NewMajorClient(m.config).QueryCollege(m)
}

// QueryClasses queries the "classes" edge of the Major entity.
func (m *Major) QueryClasses() *ClassQuery {
	return NewMajorClient(m.config).QueryClasses(m)
}

// Update returns a builder for updating this Major.
// Note that you need to call Major.Unwrap() before calling this method if this Major
// was returned from a transaction, and the transaction was committed or rolled back.
func (m *Major) Update() *MajorUpdateOne {
	return NewMajorClient(m.config).UpdateOne(m)
}

// Unwrap unwraps the Major entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (m *Major) Unwrap() *Major {
	_tx, ok := m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Major is not a transactional entity")
	}
	m.config.driver = _tx.drv
	return m
}

// String implements the fmt.Stringer.
func (m *Major) String() string {
	var builder strings.Builder
	builder.WriteString("Major(")
	builder.WriteString(fmt.Sprintf("id=%v, ", m.ID))
	builder.WriteString("cid=")
	builder.WriteString(fmt.Sprintf("%v", m.Cid))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(m.Name)
	builder.WriteString(", ")
	builder.WriteString("is_disabled=")
	builder.WriteString(fmt.Sprintf("%v", m.IsDisabled))
	builder.WriteString(", ")
	if v := m.CreatedTime; v != nil {
		builder.WriteString("created_time=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	if v := m.DeletedTime; v != nil {
		builder.WriteString("deleted_time=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	if v := m.ModifiedTime; v != nil {
		builder.WriteString("modified_time=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// Majors is a parsable slice of Major.
type Majors []*Major
